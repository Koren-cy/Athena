现在变成了五行三列了，那么接下来你要干嘛呢？画六道斜对角线。

- 一道是圈起来这三个量：`i`、`y2`、`z1`



- 每一个圈起来的斜对角线都有三个量。
- 将每个圈里的三个量乘起来。
- 第一个圈起来的斜对角线是 `y1 * z2` 倍的 `i` 向量。
- 第二个圈起来的斜对角线是 `x1 * y2`。



- `x2 * z1` 倍的 `j` 向量



- `x2 * y1` 倍的 `k` 向量
- `z1 * y2` 倍的 `i` 向量
- `x1 * z2` 倍的 `j` 向量



接下来，将左边的三个红色向量加起来，然后减去右边的三个红色向量的和。

> 注意：方向不要搞反。

- `a x b` 不等于 `b x a`



将每个圈里的三个量乘起来：

- `y1 * z2` 倍的 `i` 向量
- `x2 * z1` 倍的 `j` 向量
- `x1 * y2` 倍的 `k` 向量

然后将左边的阵营加起来，减掉右边的阵营。

向量叉乘的结果是一个新的向量，其坐标为：

- 第一个坐标：`y1 * z2 - y2 * z1`
- 第二个坐标：`x2 * z1 - x1 * z2`
- 第三个坐标：`x1 * y2 - x2 * y1`



x2 * z1 减掉 x1 * z2



第三个坐标：`x1 * y2 - x2 * y1`

向量的**长度（模长）**代表 `a` 和 `b` 向量所形成的平行四边形的面积。



向量叉乘的长度等于 `|a| * |b| * sinθ`。

向量叉乘的方向：

- 与 `a` 和 `b` 同时垂直。
- 使用右手定则判断方向：四指握拳，从 `a` 转向 `b`，大拇指方向即为叉乘结果的方向。



使用右手定则判断方向：四指握拳，从 `a` 转向 `b`，大拇指方向即为叉乘结果的方向。当然，也可以使用三个手指的右手定则。

`|a| * |b| * sinθ` 代表 `a` 和 `b` 向量所形成的平行四边形的面积。



向量的**长度（模长）**代表 `a` 和 `b` 向量所形成的平行四边形的面积。



在物理中，**力矩**是叉乘定义的。



在物理里面，**力矩**是叉乘来定义的。

- 力矩是*力* 叉乘 *力臂*。



力矩是 `r` 向量叉乘 `F` 向量的结果。

- `r` 向量：从支点指向力的作用点的向量。
- `F` 向量：力向量。



`r` 叉乘 `F` 表示力矩。



力矩的方向：

- 力矩的方向代表物体旋转的轴。
- `r` 叉乘 `F` 的结果向量指向旋转轴的方向。



物体旋转的方向也可以用一个向量来描述。



物体可以沿不同轴旋转，旋转方向不同。



当它这样转的时候，转轴是上下的。

当它这样转的时候，转轴是这样的。



四指指向旋转的方向，大拇指指向转轴的方向。

在空间里面，旋转是多维的。

`r` 叉乘 `F` 形成的平行四边形的面积就是力矩的大小。



向量叉乘与**杠杆原理**：

- **力矩**与中学物理中的**杠杆原理**相对应。
- **阻力臂**乘以**阻力**的大小等于**动力臂**乘以**动力**的大小。
- **力臂**是指作用点到力的距离。



力臂是指作用点到力的距离，乘以平行四边形的高，仍然等于平行四边形的面积。



### 向量叉乘的计算方法

如果忘记了叉乘的计算方法，可以简单地在草纸上画一个坐标系 `xyz`。

- 这个坐标系上有三个最基本的向量：`i`、`j`、`k`。



如果忘记了叉乘的计算方法，可以简单地在草纸上画一个坐标系 `xyz`。

- 这个坐标系上有三个最基本的向量：`i`、`j`、`k`。

`i` 叉乘 `j` 等于 `k`。



- `i` 叉乘 `j` 等于 `k`
- `j` 叉乘 `k` 等于 `i`
- `i` 叉乘 `k` 等于 `-j`



`i` 叉乘 `j` 等于 `k`。

- `j` 叉乘 `k` 等于 `i`。
- `i` 叉乘 `k` 等于 `-j`。

知道写这个式子是干嘛了吗？

比如说让你算 `x1y1z1` 叉乘 `x2y2z2`，你应该怎么写？

- `x1` 倍的 `i` 向量 + `y1` 倍的 `j` 向量 + `z1` 倍的 `k` 向量 叉乘 `x2...`，然后拆括号就可以了。








