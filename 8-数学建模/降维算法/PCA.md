---
tags:
  - 降维算法
  - 机器学习
  - 数据分析
  - 无监督
  - 线性
finished: false
---
# 算法简介
## 什么是PCA
主成分分析（PCA）是一种无监督的线性降维技术，旨在通过正交变换将高维数据投影到低维空间，同时最大化保留数据的方差。PCA能有效减少特征冗余、去除噪声，并便于数据可视化或后续分析，但假设数据呈线性分布且对尺度敏感，通常需先标准化处理。

>[!tip] Tip
> 简要说明什么是PCA算法，PCA算法的作用，和PCA算法的特性
## PCA解决了什么问题
- [[多重共线性]]
- 计算量大

>[!tip] Tip
> - 应使用无序列表列举问题，并通过引用解释术语含义。
> - 术语解释存放在 `8-数学建模/术语及概念/` 文件夹下。
> - 右上角开启阅读模式将鼠标置于引用之上，即可速览引用。

## 核心思想
通过一个正交线性变换，将原始高维空间中可能存在相关性的特征，转换为一组新的、彼此线性无关的特征（称为主成分）。这些主成分按照其所能解释原始数据方差的大小降序排列。PCA降维的本质是**保留最大方差的方向**，选择前k个能解释绝大部分数据方差的主成分，舍弃方差贡献较小的成分，从而在尽可能保留原始数据主要信息的前提下，实现数据维度的有效降低，以较低维度表征数据。其过程可视为寻找原始特征空间中对数据散布贡献最大的正交方向（主方向）。

## 局限性
1. **线性假设局限**
	- PCA基于线性变换，无法有效捕捉非线性数据结构（如流形、环形分布）。
2. **方差导向引起的偏差**  
	- 主成分按方差最大化选取，但高方差方向未必对应关键判别信息。
3. **正交性约束**
	- 强制主成分正交，可能扭曲数据的本征结构。真实数据的内在维度或存在非正交基，导致信息表达不自然。
4. **特征含义丢失**
	- 降维后主成分为原始特征线性组合，物理意义不明确，不利于特征可解释性需求。
5. **离群点影响**
	- 协方差矩阵对离群点敏感，可能导致主成分方向偏移。
6. **忽略类别信息**  
	- 属无监督方法，未利用样本标签。

# 数学解释
## 数学概念的直观理解

| 关键概念      | 解释                 |
| --------- | ------------------ |
| [[协方差]]   | 方差在多元问题下的一般形式      |
| [[SVD分解]] | 将数据拆解成更简单、更具解释性的成分 |
|           |                    |
## 算法流程与数学原理

# 超参数及其直观解释

# 可视化方法
1. **载荷图**
	- 展示原始特征在低维主成分空间中的方向和重要性。
2. **双标图**
	- 将得分图和载荷图叠加在同一坐标系下，同时展示样本分布和变量贡献及其关系。
3. **碎石图**
	- 显示每个主成分解释的方差以及解释方差的累积和。用于确定保留多少个主成分是合理的。
4. **热力图**
	- 可视化主成分载荷矩阵或原始变量与前K个主成分之间的相关性。
5. 累积方差解释图
	- 展示随着主成分数量增加，累积解释方差比例的变化。
6. **变量贡献图**
	- 针对单个主成分，展示各个原始变量对该主成分方差的贡献度。
# 评价指标
| 指标   | 介绍                   |
| ---- | -------------------- |
| 解释方差 | 主成分总共能够解释原始数据总方差的百分比 |
|      |                      |
# 注意事项
1. **数据标准化/归一化**
    - PCA对变量的尺度非常敏感。如果特征的单位和量纲不同，那么方差大的特征会主导主成分的方向，即使它可能并不包含最重要的信息。
2. **处理缺失值**
    - PCA算法本身不能直接处理缺失值。
3. **线性假设**
    - 如果数据中存在复杂的非线性结构（如流形结构），PCA可能无法有效地捕捉这些关系，降维效果可能不佳。
4. **方差不等于信息量**
    - 如果数据中某些方差很小的方向恰好包含了对下游任务至关重要的信息，PCA在降维过程中可能会丢失这些信息。
5. **主成分数量的选择**
	- 绘制碎石图，寻找解释方差下降速度突然变缓的“肘点”，选择“肘点”之前的主成分。
6. **主成分的解释性**
    - 主成分是原始特征的线性组合，本身可能没有直接意义。解释主成分的含义需要分析其载荷，看哪些原始特征对其贡献大。
7. **离群值敏感**
    - PCA对离群值非常敏感。少数极端离群点可能显著改变主成分的方向。
8. **无监督学习**
    - 如果目标是分类或回归，并且有标签信息`y`，那么监督降维方法可能更有效。
9. **高维稀疏数据**
    - PCA处理此类数据的效果可能不理想。